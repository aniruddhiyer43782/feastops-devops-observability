apiVersion: v1
kind: Service
metadata:
  name: feastops-app
  namespace: feastops
  labels:
    app.kubernetes.io/name: feastops-app
    app.kubernetes.io/part-of: feastops
spec:
  type: NodePort
  selector:
    app.kubernetes.io/name: feastops-app
  ports:
    - name: http
      port: 80
      targetPort: http
      nodePort: 31080
